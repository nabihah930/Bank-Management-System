
@{
    ViewBag.Title = "Expense Tracking";
    Layout = "~/Views/Shared/_WebsiteLayout.cshtml";
}
@section script{
    <script>
    </script>
}
<br />
<h2 style="text-align: center;">Expense Tracking</h2>
<br />
<div class="container" style="border: 1px solid gainsboro; border-radius: 10px; margin: 20px; padding: 20px; background-color: whitesmoke; ">
    <div class="row">
        <div class="col-md-6">
            <form action="" class="form-inline" style="margin:10px; padding-left:20px;">
                <div class="form-group mb-2">
                    <input type="text" class="form-control" id="expenseName" name="expenseName" placeholder="Expense Name">
                </div>
                <div class="form-group mx-sm-3 mb-2">
                    <input type="number" class="form-control" id="expenseAmount" name="expenseAmount" placeholder="Expense Amount">
                </div>
                <button type="submit" class="btn btn-primary mb-2">Add</button>
            </form>
        </div>
        <div class="col-md-6">
            <div class="alert alert-info alert-dismissible" style="margin-top:10px;">
                <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                <strong id="demo">Rent Expense: </strong> 50000
            </div>
            <div class="alert alert-info alert-dismissible" style="margin-top:10px;">
                <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                <strong id="demo">Utilities Expense: </strong> 7000
            </div>
            <div class="alert alert-info alert-dismissible" style="margin-top:10px;">
                <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                <strong id="demo">Repair Expense: </strong> 3000
            </div>

            <!--<p>@ViewBag.Expenses</p>
    @{
        if (ViewBag.Expenses.Count != 0)
        {
            foreach (KeyValuePair<string, float> kvp in ViewBag.Expenses)
            {
                <div class="alert alert-info alert-dismissible" style="margin-top:10px;">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    <strong id="demo">@kvp.Key: </strong> @kvp.Value
                </div>
            }
        }
    }-->
        </div>
    </div>
</div>